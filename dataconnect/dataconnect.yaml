specVersion: "v1beta"
serviceId: "AI_Deploy"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "AI_Deploy-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
